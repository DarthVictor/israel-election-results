# Build-time data tooling

This directory is reserved for typed, reproducible import and validation scripts.
They will transform preserved raw election sources into compact static assets;
the browser application will not parse source CSV files.
