# Product features

Feature modules will be added here as the explorer is built: map exploration,
locality analytics, comparisons, and the results table. They share domain types
but should not import each other's internal implementation details.
