# Product features

The election-results feature contains the map, locality analysis, comparison,
and detailed results views. Shared election state is scoped to its provider;
view-only controls stay with the component that owns them.
