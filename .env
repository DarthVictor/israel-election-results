# Canonical production origin. Not a secret: this is the public address of the
# deployment, and it is committed so `pnpm build` works from a fresh clone.
# An explicit VITE_SITE_URL in the environment (as CI sets) overrides this.
VITE_SITE_URL=https://israel-election-results.vercel.app
